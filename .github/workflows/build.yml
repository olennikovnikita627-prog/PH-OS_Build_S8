name: PH-OS Build S8
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo
        run: |
          git clone --depth=1 https://github.com/exynos8895/android_kernel_samsung_universal8895.git kernel
      

        


